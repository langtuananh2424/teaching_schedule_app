name: teaching_schedule_app
description: A demo app generated from Figma screens (Login + 4 screens)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.3
  google_fonts: ^4.0.3
  shared_preferences: ^2.1.1
  flutter_svg: ^2.0.1
  excel: ^2.1.0
  file_saver: ^0.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_dhtl.png
